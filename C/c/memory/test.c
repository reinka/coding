/*double *ptd;
ptd = (double*) malloc(30 * size of(double))*/

#include <stdio.h>
#include <iso646.h>  //for alternative spellings for the logical operators 

int main(int argc, char const *argv[])
{	
	printf("%d, %s\n", argc, argv);
	return 0;
}